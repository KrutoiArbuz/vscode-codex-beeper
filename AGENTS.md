# Git Conventions

- Every commit message must follow the Conventional Commits format: `<type>[optional scope]: <description>`.
- Every branch name must follow the conventional format `<type>/<kebab-case-description>`, using an appropriate Conventional Commits type such as `feat`, `fix`, `docs`, `refactor`, `test`, `build`, `ci`, or `chore`.
